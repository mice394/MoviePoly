#include "loadBoard.h"
#include <fstream>
#include <iostream>

#include <vector>
#include <string>
using namespace std;

#include "player.h"

void LoadBoard::init() {
    char ch;
	fstream stringin(saveFile, fstream::in);
}
